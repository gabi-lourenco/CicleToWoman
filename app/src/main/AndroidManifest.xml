<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.cicletowoman">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:name=".MyApplication"
        android:theme="@style/Theme.Design.Light.NoActionBar">
        <activity android:name="com.example.cicletowoman.activities.SignInFirebaseActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name="com.example.cicletowoman.activities.FirstPeriodActivity"
            android:theme="@style/Theme.Design.Light.NoActionBar"
            android:exported="true" />

        <activity android:name="com.example.cicletowoman.activities.StatusCycleActivity"
            android:theme="@style/Theme.Design.Light.NoActionBar"
            android:exported="true" />

        <activity android:name="com.example.cicletowoman.activities.HistoryActivity"
            android:theme="@style/Theme.Design.Light.NoActionBar"
            android:exported="true" />
    </application>
</manifest>